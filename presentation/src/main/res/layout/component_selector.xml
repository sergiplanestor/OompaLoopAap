<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/option_left"
            style="@style/Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_normal"
            android:layout_marginEnd="@dimen/margin_small"
            android:background="@drawable/shape_pill_selected"
            android:paddingStart="@dimen/margin_normal"
            android:paddingTop="@dimen/margin_tiny"
            android:paddingEnd="@dimen/margin_normal"
            android:paddingBottom="@dimen/margin_small"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/option_right"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Option 1" />


        <TextView
            android:id="@+id/option_right"
            style="@style/Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_small"
            android:layout_marginEnd="@dimen/margin_normal"
            android:background="@drawable/shape_pill_deselected"
            android:paddingStart="@dimen/margin_normal"
            android:paddingTop="@dimen/margin_tiny"
            android:paddingEnd="@dimen/margin_normal"
            android:paddingBottom="@dimen/margin_small"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/option_left"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Option 2" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
